<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <RootNamespace>MouseHouse</RootNamespace>
        <AssemblyName>MouseHouse</AssemblyName>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

        <!-- Publish settings for small binaries -->
        <PublishTrimmed>true</PublishTrimmed>
        <TrimMode>link</TrimMode>
        <PublishSingleFile>true</PublishSingleFile>
        <SelfContained>true</SelfContained>
        <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Raylib-cs" Version="6.1.1" />
        <PackageReference Include="ENet-CSharp" Version="2.4.8" />
    </ItemGroup>

    <ItemGroup>
        <Compile Remove="MouseHouse.Paint/**" />
        <None Remove="MouseHouse.Paint/**" />
        <EmbeddedResource Remove="MouseHouse.Paint/**" />
        <None Include="assets/**" CopyToOutputDirectory="PreserveNewest" />
    </ItemGroup>

    <Target Name="BuildPaintCompanion" AfterTargets="Build">
        <Exec Command="dotnet build &quot;$(MSBuildThisFileDirectory)MouseHouse.Paint/MouseHouse.Paint.csproj&quot; -c $(Configuration) -o &quot;$(OutDir)MouseHousePaint&quot;" />
    </Target>

    <Target Name="PublishPaintCompanion" AfterTargets="Publish">
        <Exec Command="dotnet publish &quot;$(MSBuildThisFileDirectory)MouseHouse.Paint/MouseHouse.Paint.csproj&quot; -c $(Configuration) -o &quot;$(PublishDir)MouseHousePaint&quot;" />
    </Target>
</Project>
